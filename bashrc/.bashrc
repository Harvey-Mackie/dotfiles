# env variables
alias GH_TOKEN=''

# alias 
alias k='kubectl'
alias kgp='k get pods'
alias k_ctx='kubectl config get-contexts && kubectl get namespaces'

alias sb='cd ~/personal-notes'
alias notes='cd ~/personal-notes'
alias repos='cd ~/repos' 
alias dotfiles='cd ~/dotfiles/'


# scripts
refresh(){
  source ~/.bashrc
}
