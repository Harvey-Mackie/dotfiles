# {REPO NAME}

![Language](https://img.shields.io/badge/language-{LANGUAGE: infer from repo}-orange)

## Overview
{OVERVIEW: exactly 3 sentences. Sentence 1: the problem this repo solves. Sentence 2: what the repo does to solve it. Sentence 3: who it is for.}

## Getting Started

### Prerequisites
{PREREQUISITES: infer from dependency files (e.g. requirements.txt, package.json, go.mod). List each with a link to official install docs. Format as a bullet list.}

### Installation
{INSTALLATION: infer the setup commands from the repo. Show only shell commands, no explanation prose.}
```sh
$ {command}
```

### Configuration
{CONFIGURATION: include this section only if env vars, config files, or secrets are present in the repo. List each variable, its purpose, and whether it is required or optional. Omit this section entirely if not applicable.}

### Usage
{USAGE: infer the primary run command from the repo. Show at least one realistic example including expected output. If flags or subcommands exist, show the most common one.}
```sh
$ {command}
// {expected output}
```

## Structure
{STRUCTURE: infer from the file tree. Include emoji folder icons. Annotate each entry with a brief inline comment. Omit node_modules, __pycache__, .git, and other non-essential directories.}
```sh
{file tree}
```

## How It Works
{DIAGRAM: generate a Mermaid diagram that best represents this repo. Use a sequence diagram for APIs, flowchart for CLIs and scripts, and a simple graph for config/dotfile repos. Annotate nodes clearly.}

## References
{REFERENCES: include only if external libraries, papers, or repos meaningfully influenced this project. Format as a markdown bullet list with linked titles. Omit this section entirely if not applicable.}

